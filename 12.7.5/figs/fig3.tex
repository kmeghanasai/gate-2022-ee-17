\begin{circuitikz}
    % Draw the components and connect them
    \draw (0,2) to[C, l=$60\,\mu F$] (4,2)
    (0,0) to[V, v=$110\,V$, f=$60\,Hz$] (4,0)
    (0,2) -- (0,0)
    (4,2) -- (4,0);
\end{circuitikz}
